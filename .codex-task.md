# タスク: 凍結した onboarding-sync.ps1 の自己更新 と インストーラの破壊的置換ガード

## 背景（2026-08-24 実測）
メンバーPC(別アカウント)で `~/orgiast-claude-rules` が **git リポジトリではない古い配布コピー**で、
`tools/` に `register-hooks.mjs` も `onboarding-sync.mjs` も存在せず（`onboarding-sync.ps1` のみ）、
`~/.claude/skills/` 自体が存在しなかった。つまり配布が一度も届いていない。

原因: SessionStart に登録されているのは `~/.claude/hooks/onboarding-sync.ps1` の**凍結コピー**で、
リポジトリ側の `tools/onboarding-sync.ps1` がいくら改善されても、**その凍結コピー自身は誰も更新しない**。
リポ同期(zip で tools/rules-extracted/skills を更新)を通っても、hooks 配下の自分自身は古いまま残り続ける。

## 実装するもの

### 1) `tools/onboarding-sync.ps1` — 自分自身(hooks配下の凍結コピー)を更新する
- リポ同期が成功した直後（`Write-SyncLog "repo updated ($repoSyncMethod)"` の後）に、
  `$repoRoot\tools\onboarding-sync.ps1` を `$homeRoot\.claude\hooks\onboarding-sync.ps1` へ
  **中身が違うときだけ**コピーする。
  - 同一判定はバイト比較（`[System.IO.File]::ReadAllBytes` の比較、または長さ＋ハッシュ）。
  - コピー先が存在しない場合はコピーしない（この端末では hooks 方式を使っていないため。勝手に増やさない）。
  - 実行中の自分自身を上書きする形になるが、PowerShell はスクリプトを読み込み済みなので問題ない。
    **次回セッションから新版が有効**になる旨を `Write-SyncLog` に記録する。
  - コピー成功/失敗どちらでも後続処理を止めない（try/catch で握る。ログには残す）。
- `$homeRoot` はこのブロックより後で定義されている場合があるので、必要なら
  同じ導出（`$env:ORGIAST_HOME` → `$env:USERPROFILE` → `[Environment]::GetFolderPath('UserProfile')`）を
  ローカル変数で行う。既存の変数名・既存の挙動は変えない。
- **BOM 付き UTF-8 で書き出すこと**（このリポの .ps1 は日本語を含み BOM 前提。`Set-Content -Encoding UTF8` 相当）。
  バイトコピー（`Copy-Item`）ならエンコーディングは変わらないので `Copy-Item` を使うのが安全。

### 2) `tools/install-orgiast.ps1` — 未コミットの作業を消さないガード
現状 `if (Test-Path $REPO) { Remove-Item $REPO -Recurse -Force }` → `Move-Item` で
**リポジトリを丸ごと消して置き換える**。メンバーPCでは正しいが、開発機で実行すると
未コミットの作業が消える（kim機は `~/orgiast-claude-rules` が `Downloads\orgiast-claude-rules` への
ジャンクションで、実体は開発用の作業ツリー）。
- zip 展開の前に判定を追加する:
  - `$REPO\.git` が存在し、かつ `git -C $REPO status --porcelain` の出力が空でない場合は
    **削除・置換を行わず**、`git -C $REPO pull --ff-only` を試みる。
    成功/失敗どちらでも `$gotRepo = $true`（既存を使用）とし、
    `Warn "未コミットの変更があるため既存リポジトリを保持しました(削除せず pull のみ)"` を出す。
  - それ以外（.git が無い＝配布コピー、または clean な作業ツリー）は**現状どおり**削除して置き換える。
- 既存のメッセージ・成功時の挙動は変えない。git が無い環境では判定をスキップして現状どおり。

### 3) `tools/selftest-guards.mjs` にガードを2本追加
このリポの既存スタイル（ソース文字列を機械的に検査する PASS/FAIL 形式）に合わせて追加する:
- `onboarding-sync.ps1: hooks配下の自分自身を更新する` —
  `tools/onboarding-sync.ps1` の中身に、hooks 配下の `onboarding-sync.ps1` へコピーする処理が
  含まれることを文字列で検査（例: `.claude\hooks` と `onboarding-sync.ps1` と `Copy-Item` が
  同一ブロックに現れる、など実装に合わせた確実な検査）。
- `install-orgiast.ps1: 未コミットの作業ツリーを削除しない` —
  `Remove-Item $REPO -Recurse` の前に `status --porcelain` による分岐が存在することを検査。
- 既存のガードの並び・出力形式（`PASS <名前>` / 最後に `ALL PASS`）を壊さない。

## 制約
- 変更するのは上記3ファイルのみ。スコープ外を触らない。
- 既存の挙動・ログ文言を壊さない（追加のみ）。
- 実装後に `node tools/selftest-guards.mjs` が ALL PASS になることを確認する。
- `node --test tools/*.test.mjs` も通ること（112 pass が現状）。
