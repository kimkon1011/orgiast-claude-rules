# AWS を月3万円で構築する — 実現性の検証と要件確認

対象提案: AIニュース提案 **P-0105**（`~/.claude/ai-news-proposals.jsonl`）
元発言: 「参加者はAWSを月3万円で構築できるか疑問を呈した。」（LINEオープンチャット、2026-09-18 03:20 の digest）
検証日: 2026-09-19 / 価格基準日: 2026-09-19

見積もりは `tools/aws-cost-estimate.mjs` で再計算できる（価格表・為替・予算を差し替え可能）。

```bash
node tools/aws-cost-estimate.mjs                      # 全プラン
node tools/aws-cost-estimate.mjs --plan C --jpy-rate 160 --budget 30000
```

---

## 1. 結論（先に3行）

1. **月3万円は余裕で収まる。** ¥30,000 = 約 $192（@¥156）。Lightsail の固定月額だけで組むと、
   冗長化込みの C案（16GB + DB HA + LB + スナップショット）でも **¥21,700/月**。
2. **ただし「3万円で何ができるか」は要件が決まらないと確定しない。** 元発言には
   作りたいもの・利用者数・可用性要件が無い。§4 のチェックリストが埋まって初めて見積もりが確定する。
3. **無料枠の理解に注意。** 2025-07-15 以降に作ったアカウントは従来の「12ヶ月無料」ではない（§3）。

## 2. 見積もり（一次情報の価格のみ / 東京リージョン）

| プラン | 構成 | USD/月 | 月額(¥156/$) | 予算超過 |
| --- | --- | ---: | ---: | --- |
| A 最小（学習・個人開発） | Small 2GB + DB 1GB + snapshot 40GB | $28.72 | **¥4,480** | −¥25,520 |
| B 標準（小規模本番・単一AZ） | Medium 4GB + DB + LB + snapshot 80GB | $60.38 | **¥9,419** | −¥20,581 |
| C 冗長化（中規模・DB HA） | Xlarge 16GB + DB HA + LB + snapshot 160GB | $139.10 | **¥21,700** | −¥8,300 |
| **E EC2（2026年内）** | **t4g.small 無料枠 750h/月** + DB 1GB + snapshot 40GB | $16.72 | **¥2,608** | −¥27,392 |
| D 予算上限の確認 | メモリ最適化 2Xlarge 64GB + DB 4GB + LB | $370.53 | **¥57,803** | **+¥27,803** |

**E案の注意**: AWS 公式ページに *"t4g.small instances ... free for up to 750 hours / month until Dec 31st 2026"* と明記されている
**期限付き**の無料枠。2026-12-31 を過ぎると同じ構成でもこの金額では済まない（ツールは `⏳ 期限付き` を必ず表示する）。

**3万円の境界線**: Lightsail インスタンスで 16GB（$84）までなら DB/LB/バックアップを足しても予算内（C案 ¥21,700）。
32GB（$164）に上げると DB 1GB（$14.72）と LB（$17.66）を足した時点で **$196.38 = ¥30,635** となり予算を超える。
メモリ最適化 64GB（$294）は単体で予算超過。

価格の出典（すべて一次情報）:
- Lightsail インスタンス月額: <https://docs.aws.amazon.com/lightsail/latest/userguide/amazon-lightsail-bundles.html>
- Lightsail 東京リージョン実額（DB / LB / snapshot / 静的IP）: AWS 公開プライスリスト
  `https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonLightsail/current/index.json`（`regionCode=ap-northeast-1`）を
  実測。例: DB 1GB $0.02016/h → ×730h = $14.72、LB $0.024193548/h → $17.66、snapshot $0.05/GB-Mo、未アタッチ静的IPv4 $0.005/h。
- 為替: 日銀 東京市場 仲値 2026-09-01〜09-15 の平均 ≈155.8 JPY/USD（既定 156）。

## 3. 無料枠の正しい理解（ここが一番の落とし穴）

- **新規アカウント（2025-07-15 以降）は「12ヶ月無料」ではない。** サインアップ時に
  **Free account plan** か **Paid account plan** を選ぶ方式に変わった。
  - Free plan: 最長6ヶ月、$100 クレジット。**6ヶ月経過 or クレジット使い切りで終了**（アカウントは閉じられる）。
  - Paid plan: 全サービス利用可。クレジット超過分は従量課金。
- どのプランでも **$100 のサインアップクレジット**、さらにアクティビティ達成で **最大 $100 追加**（合計最大 $200）。
- **Always Free**（月次上限つきで永続）が 30 サービス以上ある。Lambda / CloudFront などが該当。
- **期限付きの無料枠もある。** EC2 の `t4g.small` は **2026-12-31 まで月750時間まで無料**
  （出典: <https://aws.amazon.com/ec2/instance-types/t4/>）。750時間はほぼ1台分（730h/月）なので、
  **2026年内に限り**「EC2 1台 + Lightsail マネージドDB」が月 $16.72（¥2,608）で組める（E案）。
  期限後は通常料金に戻るため、年をまたぐ構成では E案をそのまま使ってはいけない。
- ⚠️ **Always Free の各サービスの具体的な月次上限は本調査では一次情報で確認できていない。**
  0円と断定せず、実際に使う際はアカウントの Free Tier ページで確認すること。
  （`tools/aws-cost-estimate.mjs` の Lambda / CloudFront 行は `usd: 0` だが、これは
  「上限内なら無料」の意味であり、超過時の実費は別途見積もりが必要。）

## 4. 要件確認チェックリスト（これが埋まるまで見積もりは確定しない）

元発言には**作りたいものが書かれていない**ため、以下を確認して初めて構成が決まる。

| # | 確認項目 | 選択肢 | 見積もりへの効き方 |
| --- | --- | --- | --- |
| 1 | 何を作るか | 静的サイト / Web API / DB付きWebアプリ / バッチ / 機械学習 | インスタンスサイズとDB要否が決まる |
| 2 | 想定利用者数・同時接続 | 〜10 / 〜100 / 〜1,000 / それ以上 | 2GB〜16GB の幅（$12〜$84）が決まる |
| 3 | 可用性 | 落ちてよい / 業務時間だけ / 24h無停止 | DB HA（+$14.72）と LB（+$17.66）の要否 |
| 4 | データ量と保持 | GB / 転送量 / バックアップ世代数 | スナップショット $0.05/GB-Mo が効く |
| 5 | リージョン | 東京 / 大阪 / 米国 | 単価とレイテンシが変わる |
| 6 | 既存の契約 | AWSアカウントの有無・作成時期 | **2025-07-15 以前なら従来の12ヶ月無料が使える**（§3） |
| 7 | 運用体制 | 自力運用 / マネージド希望 | Lightsail（固定額・簡単）か EC2/RDS（柔軟・従量）の分岐 |
| 8 | GPU / 大規模推論の要否 | 要 / 不要 | **要なら月3万円では足りない**（Lightsail GPU 最安 Std_XL_8GB $1.13/h → 月$824.9 = ¥128,684） |

## 5. 未確認・未計測として残したもの（結論の限界）

正直に分離しておく。以下は**確定値ではない**。

- **EC2 / RDS の東京リージョン実額は一次情報で取得できなかった。** 第三者トラッカーの値
  （EC2 t4g.medium 東京 $0.0432/h、RDS db.t4g.micro us-east-1 $0.016/h）は `verified: false` として
  カタログに入れてあり、合計とは別枠で集計される。EC2 で組む場合は AWS Pricing Calculator で取り直すこと。
- Always Free の各サービスの月次上限（§3）。
- データ転送の超過料金（Lightsail は各プランに 1〜10TB が含まれるが、超過時 $0.09/GB〜）。
- 消費税・為替変動。為替は `--jpy-rate` で差し替え可能。¥150 なら C案は ¥20,865、¥165 なら ¥22,952。

## 6. triage `finding` の検証結果

`ai-news-proposals.jsonl` の P-0105 の `finding` は
「検索結果は、AWSを月3万円以内で構築可能で、構成例と要件確認項目を示しており、提案内容を裏付けている」であり、
**具体的な数値が一切入っていない**。したがって数値の誤りは無いが、**結論の根拠としても使えない**
（一般論の要約にすぎない）。本ドキュメントの数値はすべて上記の一次情報から取り直したものである。
