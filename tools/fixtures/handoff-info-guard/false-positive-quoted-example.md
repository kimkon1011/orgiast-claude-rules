**恒久化まで確認しました。**

## 検証結果（すべて正本から降りてきた版で実施）

| 確認 | 結果 |
|---|---|
| 正本 main に修正が入ったか | **PR #148 / `0aa5c67`** |
| 同期で消えないか | ローカルを**削除 → 同期 → 修正版が復活**（9,510 bytes・修正版であることを文字列で照合） |
| 誤爆の回帰 | **実際にブロックされた応答の全文を再判定 → 素通し** |
| 過小検知していないか | 「次のコマンドを実行してください。」だけの依頼 → **ちゃんと止まる** |
| テスト | selftest 7件 / 契約テスト5件 → 通過 |

**他の PC / 他のアカウントにも、次回セッション開始時の同期で修正版が入ります。**

## 今回の一連で残ったもの

- **仕組み**: 手作業依頼が「場所・番号付き手順3つ以上・成功の目印・失敗時の逃げ道」を満たさないと応答が止まる（全社共通・自動配布・自動登録）。逃がし弁は `[MANUAL-OK]`。
- **教訓の記録**: memory に2件（[feedback_manual_request_full_steps_gate](../../.claude/projects/c--Users-kimko-nf-minpaku-automation/memory/feedback_manual_request_full_steps_gate.md) ほか）＋民泊リポの [tools/hooks/](tools/hooks/)。特に「**ローカルに置いた＝効いている、ではない**」（同期で消え、登録だけ残って存在しないファイルを呼ぶ状態になっていた）を手順込みで残しました。
- **副産物**: hook は「どこを読むか」で壊れるので、判定関数の単体テストではなく **stdin→stdout の契約テスト**を書く型ができました（実際それが「1つ前のターンを読む」不具合を捕まえました）。

## 正直に残す反省点

1. **ルールがあるのに破った**のが発端で、しかも**導入した hook 自身が初日に誤爆**しました。どちらも「部分一致で言及と実行を区別できない」という**既に memory にあった罠**の再発です。今回はテストを実メッセージでの回帰まで含めたので、同じ壊れ方は次から検出できます。
2. **最初に権限を確認せず**「`gh auth login`」という的外れな依頼をしました。以後、人に頼む前に `--dry-run` 相当で1回試します。
3. 保護ブランチのため、ルール更新のたびに**マージのクリックだけは残ります**。これは安全側の設計として妥当なので、そのままにしています。

本セッションの目的（ルールの徹底）は完了です。`/session-close` を推奨します。次セッションの本命は引き継ぎのとおり、8/28 の特別日反映確認と残室連動（案B）の続きです。